# Security and privacy reports

Do not post sensitive data in public issues. For suspected vulnerabilities, use GitHub's private vulnerability reporting if available through the Security tab. Otherwise open an issue requesting a private contact channel without describing the exploit or attaching personal data.

This beta stores its catalog, thumbnails, names, and embeddings locally without application-level encryption. Consult the README privacy section before using it with sensitive collections.
